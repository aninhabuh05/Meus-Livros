// O JavaScript agora apenas garante que o site funciona.
// A animação foi movida para o CSS para maior fluidez e controle.
document.addEventListener('DOMContentLoaded', () => {
    console.log('Site carregado!');
});